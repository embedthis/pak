#
#   gitlab-ci.yml -- GitLab CI/CD build instructions
#

image: runner:current

variables:
    PROFILE: "release"
    SECRET_STORE: "embedthis.config/embedthis"

include: "paks/assist/makeme-build.yml"

stages:
    - build
    - publish
