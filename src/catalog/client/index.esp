<!DOCTYPE html>
<html lang="en" xng-csp>
<head>
    <title>Pak Catalog</title>
    <meta charset="utf-8" />
    <link rel="icon" href="/assets/favicon.ico" type="image/x-icon">
    <% stylesheets(NULL); %>
</head>
<body data-config='<% renderConfig(); %>' id='body' ng-cloak>
    <main class="container">
        <nav class="status" ng-controller="PakControl">
            <a href="#/user/{{Esp.user ? 'logout' : 'login'}}"> 
                {{username()}} 
                <i class="fa fa-sign-out"></i> 
            </a>
        </nav>
        <section class="col-md-12 content">
            <div ng-view></div>
        </section>
    </main>
    <div ng-repeat="(field, msg) in feedback">
        <span class="ng-hide-remove" ng-class="'feedback-' + field">{{msg}}</span>       
    </div> 
    <% scripts(NULL); %>
</body>
</html>
