/*
    start.me - MakeMe file 
 */

Me.load({
    targets: {
        postcache: {
            action: `
                trace('Build', 'Version.es')
                Cmd.run('ejsc Version.es')
                let dest = App.home.join('.ejs', 'ejs.version.mod')
                trace('Install', dest)
                cp('ejs.version.mod', dest)
            `,
        },
    },
})
